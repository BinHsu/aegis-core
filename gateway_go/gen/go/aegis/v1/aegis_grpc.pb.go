// =============================================================================
// Aegis Core — v1 contracts
//
// This file is the SINGLE SOURCE OF TRUTH for every gRPC contract in Aegis
// Core. It generates C++ bindings for the engine, Go bindings for the
// gateway, and TypeScript bindings for the frontend. Breaking changes must
// be accompanied by a new ADR and a major version bump (v1 -> v2) in a new
// `proto/aegis/v2/` directory; see `docs/adr/` and `buf.yaml`.
//
// Two services are defined:
//   (1) `aegis.v1.Gateway`  — client-facing, hosted by the Go BFF. End users
//                             (host and viewers) call this service over
//                             gRPC-Web (Cloud) or WebSocket + Protobuf
//                             (Local mode, per ADR-0007). Cognito JWT auth
//                             in Cloud mode; dummy local auth otherwise.
//   (2) `aegis.v1.Engine`   — internal, hosted by the C++ engine. Called
//                             ONLY by the Go Gateway over mTLS gRPC inside
//                             the cluster (or loopback in Local mode).
//                             End users never reach this service.
//
// Load-bearing design notes (DO NOT change without a new ADR):
//   * Raw audio PCM lives ONLY in the Engine process RAM for the
//     lifetime of a single StreamTranscribe stream. See ADR-0005
//     (seven enforcement requirements). Aegis does NOT process
//     voiceprint / biometric data at all — see ADR-0012.
//   * Transcript segments are never persisted server-side. The Go Gateway
//     is a stateless fan-out relay; see ADR-0004.
//   * Speaker labels in transcripts may be anonymous (Speaker_0..N) or
//     role-based (Host, Client, Colleague). Never real names; see
//     ARCHITECTURE.md §9.2.
//   * Viewer access control is invite-token-based for MVP. The
//     `allowed_viewer_account_ids` field on CreateMeetingRequest is
//     RESERVED for the Phase 5+ enterprise tier per ADR-0001 "Future
//     Extension Path".
//   * The `IngestMessage.pcm` field carries sensitive audio bytes. Any
//     log formatter, trace exporter, or debug tool that touches this
//     field must be type-guarded per ADR-0005 R3 and R4. Semgrep rules
//     in `tools/ci/semgrep_rules/` enforce this.
// =============================================================================

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: aegis/v1/aegis.proto

package aegisv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Gateway_CreateMeeting_FullMethodName   = "/aegis.v1.Gateway/CreateMeeting"
	Gateway_NegotiateWebRTC_FullMethodName = "/aegis.v1.Gateway/NegotiateWebRTC"
	Gateway_JoinAsViewer_FullMethodName    = "/aegis.v1.Gateway/JoinAsViewer"
	Gateway_EndMeeting_FullMethodName      = "/aegis.v1.Gateway/EndMeeting"
	Gateway_SendOfficerHint_FullMethodName = "/aegis.v1.Gateway/SendOfficerHint"
	Gateway_ListCorpora_FullMethodName     = "/aegis.v1.Gateway/ListCorpora"
)

// GatewayClient is the client API for Gateway service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type GatewayClient interface {
	// Creates a new meeting. The authenticated caller becomes the host.
	//
	// Returns the session identifier, a short-lived JWT join token (per
	// ADR-0001 Option B) that viewers present via the invite URL, and
	// metadata about the engine backing this session.
	//
	// After this call, the host must:
	//
	//	(1) Call `NegotiateWebRTC` to establish the audio path.
	//	(2) Call `JoinAsViewer` with the returned session_id to subscribe
	//	    to the live transcript and prompter stream. The host
	//	    subscribes via the same RPC as viewers.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing / invalid Cognito JWT (Cloud mode)
	//	PERMISSION_DENIED     — caller cannot access the requested RAG
	//	NOT_FOUND             — rag_id does not exist
	//	RESOURCE_EXHAUSTED    — engine ResourceBudget full (see ADR-0010)
	//	FAILED_PRECONDITION   — Gateway pod is draining (see ADR-0006)
	CreateMeeting(ctx context.Context, in *CreateMeetingRequest, opts ...grpc.CallOption) (*CreateMeetingResponse, error)
	// Establishes the WebRTC audio path between the host and the Gateway
	// via non-trickle SDP exchange. The request carries a complete offer
	// (including all ICE candidates gathered after `iceGatheringState`
	// reaches "complete"); the response carries the Gateway's complete
	// answer.
	//
	// MVP does NOT support trickle ICE. The client MUST wait for ICE
	// gathering to complete before calling. Trickle-ICE support may be
	// added in Phase 2 as a separate bidi-streaming RPC without breaking
	// this unary one.
	//
	// Errors:
	//
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session is not in ACTIVE state
	//	INVALID_ARGUMENT      — malformed SDP offer
	//	RESOURCE_EXHAUSTED    — engine budget cannot accommodate this session
	NegotiateWebRTC(ctx context.Context, in *NegotiateWebRTCRequest, opts ...grpc.CallOption) (*NegotiateWebRTCResponse, error)
	// Subscribes to the live event stream for a meeting. Used by both the
	// host (on their own session) and any viewer (via invite token).
	//
	// The stream delivers transcript segments, prompter hints, and meeting
	// state changes. The server closes the stream when the meeting ends
	// (host disconnects per ADR-0006, crashes per L1/L2, or explicitly
	// ends via EndMeeting).
	//
	// Late joiners see only events produced AFTER they join. This is an
	// intentional privacy feature per ARCHITECTURE.md §9.6 / §11 L4; it
	// must not be "fixed" to replay history.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing viewer_token
	//	PERMISSION_DENIED     — token invalid, expired, or allowlist miss
	//	                        (Phase 5+ enterprise tier)
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	JoinAsViewer(ctx context.Context, in *JoinAsViewerRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ViewerEvent], error)
	// Explicitly ends a meeting. Only the host may call this. Terminates
	// the session on the engine, disconnects all viewers with a final
	// MeetingStateChange event, and releases ResourceBudget.
	//
	// Errors:
	//
	//	PERMISSION_DENIED     — caller is not the host
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	EndMeeting(ctx context.Context, in *EndMeetingRequest, opts ...grpc.CallOption) (*EndMeetingResponse, error)
	// Staff-authored hint broadcast. The chief-of-staff types a short
	// suggestion (override a wrong RAG hit, inject an urgent fact the
	// retriever can't know) and the gateway fans it out via
	// `Session.Broadcast` as a `ViewerEvent.hint` with the chosen
	// urgency — same on-wire shape as an engine-origin retriever hint,
	// which means the viewer / host render paths don't need a second
	// code path. Differentiation at the UI is urgency-driven (LOW/NORMAL
	// render inline; HIGH/URGENT pin as a banner).
	//
	// MVP auth posture: any caller holding a valid session token may
	// send (same check as `JoinAsViewer`). Host-only enforcement
	// requires Cognito JWT role claims and lands with the Phase 5
	// enterprise auth sprint — tracked as a Known Gap in ROADMAP.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing viewer_token
	//	INVALID_ARGUMENT      — missing session_id, empty suggestion,
	//	                        or UNSPECIFIED urgency
	//	PERMISSION_DENIED     — invalid / expired token for this session
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	SendOfficerHint(ctx context.Context, in *SendOfficerHintRequest, opts ...grpc.CallOption) (*SendOfficerHintResponse, error)
	// Lists RAG corpora available to this caller. Backs the Host UI's
	// "pick a corpus" dropdown — the UI calls this on mount, populates
	// options from the response, and falls back to an empty list on
	// error. See ADR-0023 §"Decision B — RAG opt-in".
	//
	// Phase 3 (LAN / single-tenant): the gateway ignores `tenant_id` on
	// the wire and hardcodes `"demo"` when forwarding to engine —
	// ensures every `aegis_demo_*` collection in the local Qdrant is
	// surfaced.
	//
	// Phase 4+ (cloud / multi-tenant per ADR-0022 §Decision): gateway
	// MUST extract tenant_id from the JWT and substitute it; the field
	// on the wire is advisory — clients that send a mismatched
	// tenant_id are silently normalized. This keeps malicious / curious
	// clients from enumerating other tenants' collections even if the
	// field looks permissive.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing Cognito JWT (Cloud mode)
	//	UNAVAILABLE           — engine unreachable
	ListCorpora(ctx context.Context, in *ListCorporaRequest, opts ...grpc.CallOption) (*ListCorporaResponse, error)
}

type gatewayClient struct {
	cc grpc.ClientConnInterface
}

func NewGatewayClient(cc grpc.ClientConnInterface) GatewayClient {
	return &gatewayClient{cc}
}

func (c *gatewayClient) CreateMeeting(ctx context.Context, in *CreateMeetingRequest, opts ...grpc.CallOption) (*CreateMeetingResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CreateMeetingResponse)
	err := c.cc.Invoke(ctx, Gateway_CreateMeeting_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gatewayClient) NegotiateWebRTC(ctx context.Context, in *NegotiateWebRTCRequest, opts ...grpc.CallOption) (*NegotiateWebRTCResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(NegotiateWebRTCResponse)
	err := c.cc.Invoke(ctx, Gateway_NegotiateWebRTC_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gatewayClient) JoinAsViewer(ctx context.Context, in *JoinAsViewerRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ViewerEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &Gateway_ServiceDesc.Streams[0], Gateway_JoinAsViewer_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[JoinAsViewerRequest, ViewerEvent]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Gateway_JoinAsViewerClient = grpc.ServerStreamingClient[ViewerEvent]

func (c *gatewayClient) EndMeeting(ctx context.Context, in *EndMeetingRequest, opts ...grpc.CallOption) (*EndMeetingResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EndMeetingResponse)
	err := c.cc.Invoke(ctx, Gateway_EndMeeting_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gatewayClient) SendOfficerHint(ctx context.Context, in *SendOfficerHintRequest, opts ...grpc.CallOption) (*SendOfficerHintResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SendOfficerHintResponse)
	err := c.cc.Invoke(ctx, Gateway_SendOfficerHint_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gatewayClient) ListCorpora(ctx context.Context, in *ListCorporaRequest, opts ...grpc.CallOption) (*ListCorporaResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListCorporaResponse)
	err := c.cc.Invoke(ctx, Gateway_ListCorpora_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GatewayServer is the server API for Gateway service.
// All implementations should embed UnimplementedGatewayServer
// for forward compatibility.
type GatewayServer interface {
	// Creates a new meeting. The authenticated caller becomes the host.
	//
	// Returns the session identifier, a short-lived JWT join token (per
	// ADR-0001 Option B) that viewers present via the invite URL, and
	// metadata about the engine backing this session.
	//
	// After this call, the host must:
	//
	//	(1) Call `NegotiateWebRTC` to establish the audio path.
	//	(2) Call `JoinAsViewer` with the returned session_id to subscribe
	//	    to the live transcript and prompter stream. The host
	//	    subscribes via the same RPC as viewers.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing / invalid Cognito JWT (Cloud mode)
	//	PERMISSION_DENIED     — caller cannot access the requested RAG
	//	NOT_FOUND             — rag_id does not exist
	//	RESOURCE_EXHAUSTED    — engine ResourceBudget full (see ADR-0010)
	//	FAILED_PRECONDITION   — Gateway pod is draining (see ADR-0006)
	CreateMeeting(context.Context, *CreateMeetingRequest) (*CreateMeetingResponse, error)
	// Establishes the WebRTC audio path between the host and the Gateway
	// via non-trickle SDP exchange. The request carries a complete offer
	// (including all ICE candidates gathered after `iceGatheringState`
	// reaches "complete"); the response carries the Gateway's complete
	// answer.
	//
	// MVP does NOT support trickle ICE. The client MUST wait for ICE
	// gathering to complete before calling. Trickle-ICE support may be
	// added in Phase 2 as a separate bidi-streaming RPC without breaking
	// this unary one.
	//
	// Errors:
	//
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session is not in ACTIVE state
	//	INVALID_ARGUMENT      — malformed SDP offer
	//	RESOURCE_EXHAUSTED    — engine budget cannot accommodate this session
	NegotiateWebRTC(context.Context, *NegotiateWebRTCRequest) (*NegotiateWebRTCResponse, error)
	// Subscribes to the live event stream for a meeting. Used by both the
	// host (on their own session) and any viewer (via invite token).
	//
	// The stream delivers transcript segments, prompter hints, and meeting
	// state changes. The server closes the stream when the meeting ends
	// (host disconnects per ADR-0006, crashes per L1/L2, or explicitly
	// ends via EndMeeting).
	//
	// Late joiners see only events produced AFTER they join. This is an
	// intentional privacy feature per ARCHITECTURE.md §9.6 / §11 L4; it
	// must not be "fixed" to replay history.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing viewer_token
	//	PERMISSION_DENIED     — token invalid, expired, or allowlist miss
	//	                        (Phase 5+ enterprise tier)
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	JoinAsViewer(*JoinAsViewerRequest, grpc.ServerStreamingServer[ViewerEvent]) error
	// Explicitly ends a meeting. Only the host may call this. Terminates
	// the session on the engine, disconnects all viewers with a final
	// MeetingStateChange event, and releases ResourceBudget.
	//
	// Errors:
	//
	//	PERMISSION_DENIED     — caller is not the host
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	EndMeeting(context.Context, *EndMeetingRequest) (*EndMeetingResponse, error)
	// Staff-authored hint broadcast. The chief-of-staff types a short
	// suggestion (override a wrong RAG hit, inject an urgent fact the
	// retriever can't know) and the gateway fans it out via
	// `Session.Broadcast` as a `ViewerEvent.hint` with the chosen
	// urgency — same on-wire shape as an engine-origin retriever hint,
	// which means the viewer / host render paths don't need a second
	// code path. Differentiation at the UI is urgency-driven (LOW/NORMAL
	// render inline; HIGH/URGENT pin as a banner).
	//
	// MVP auth posture: any caller holding a valid session token may
	// send (same check as `JoinAsViewer`). Host-only enforcement
	// requires Cognito JWT role claims and lands with the Phase 5
	// enterprise auth sprint — tracked as a Known Gap in ROADMAP.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing viewer_token
	//	INVALID_ARGUMENT      — missing session_id, empty suggestion,
	//	                        or UNSPECIFIED urgency
	//	PERMISSION_DENIED     — invalid / expired token for this session
	//	NOT_FOUND             — unknown session_id
	//	FAILED_PRECONDITION   — session already ended
	SendOfficerHint(context.Context, *SendOfficerHintRequest) (*SendOfficerHintResponse, error)
	// Lists RAG corpora available to this caller. Backs the Host UI's
	// "pick a corpus" dropdown — the UI calls this on mount, populates
	// options from the response, and falls back to an empty list on
	// error. See ADR-0023 §"Decision B — RAG opt-in".
	//
	// Phase 3 (LAN / single-tenant): the gateway ignores `tenant_id` on
	// the wire and hardcodes `"demo"` when forwarding to engine —
	// ensures every `aegis_demo_*` collection in the local Qdrant is
	// surfaced.
	//
	// Phase 4+ (cloud / multi-tenant per ADR-0022 §Decision): gateway
	// MUST extract tenant_id from the JWT and substitute it; the field
	// on the wire is advisory — clients that send a mismatched
	// tenant_id are silently normalized. This keeps malicious / curious
	// clients from enumerating other tenants' collections even if the
	// field looks permissive.
	//
	// Errors:
	//
	//	UNAUTHENTICATED       — missing Cognito JWT (Cloud mode)
	//	UNAVAILABLE           — engine unreachable
	ListCorpora(context.Context, *ListCorporaRequest) (*ListCorporaResponse, error)
}

// UnimplementedGatewayServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGatewayServer struct{}

func (UnimplementedGatewayServer) CreateMeeting(context.Context, *CreateMeetingRequest) (*CreateMeetingResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method CreateMeeting not implemented")
}
func (UnimplementedGatewayServer) NegotiateWebRTC(context.Context, *NegotiateWebRTCRequest) (*NegotiateWebRTCResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method NegotiateWebRTC not implemented")
}
func (UnimplementedGatewayServer) JoinAsViewer(*JoinAsViewerRequest, grpc.ServerStreamingServer[ViewerEvent]) error {
	return status.Errorf(codes.Unimplemented, "method JoinAsViewer not implemented")
}
func (UnimplementedGatewayServer) EndMeeting(context.Context, *EndMeetingRequest) (*EndMeetingResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method EndMeeting not implemented")
}
func (UnimplementedGatewayServer) SendOfficerHint(context.Context, *SendOfficerHintRequest) (*SendOfficerHintResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method SendOfficerHint not implemented")
}
func (UnimplementedGatewayServer) ListCorpora(context.Context, *ListCorporaRequest) (*ListCorporaResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method ListCorpora not implemented")
}
func (UnimplementedGatewayServer) testEmbeddedByValue() {}

// UnsafeGatewayServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GatewayServer will
// result in compilation errors.
type UnsafeGatewayServer interface {
	mustEmbedUnimplementedGatewayServer()
}

func RegisterGatewayServer(s grpc.ServiceRegistrar, srv GatewayServer) {
	// If the following call pancis, it indicates UnimplementedGatewayServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Gateway_ServiceDesc, srv)
}

func _Gateway_CreateMeeting_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateMeetingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServer).CreateMeeting(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Gateway_CreateMeeting_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServer).CreateMeeting(ctx, req.(*CreateMeetingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _Gateway_NegotiateWebRTC_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(NegotiateWebRTCRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServer).NegotiateWebRTC(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Gateway_NegotiateWebRTC_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServer).NegotiateWebRTC(ctx, req.(*NegotiateWebRTCRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _Gateway_JoinAsViewer_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(JoinAsViewerRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(GatewayServer).JoinAsViewer(m, &grpc.GenericServerStream[JoinAsViewerRequest, ViewerEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Gateway_JoinAsViewerServer = grpc.ServerStreamingServer[ViewerEvent]

func _Gateway_EndMeeting_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EndMeetingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServer).EndMeeting(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Gateway_EndMeeting_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServer).EndMeeting(ctx, req.(*EndMeetingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _Gateway_SendOfficerHint_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SendOfficerHintRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServer).SendOfficerHint(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Gateway_SendOfficerHint_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServer).SendOfficerHint(ctx, req.(*SendOfficerHintRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _Gateway_ListCorpora_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListCorporaRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GatewayServer).ListCorpora(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Gateway_ListCorpora_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GatewayServer).ListCorpora(ctx, req.(*ListCorporaRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Gateway_ServiceDesc is the grpc.ServiceDesc for Gateway service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Gateway_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "aegis.v1.Gateway",
	HandlerType: (*GatewayServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "CreateMeeting",
			Handler:    _Gateway_CreateMeeting_Handler,
		},
		{
			MethodName: "NegotiateWebRTC",
			Handler:    _Gateway_NegotiateWebRTC_Handler,
		},
		{
			MethodName: "EndMeeting",
			Handler:    _Gateway_EndMeeting_Handler,
		},
		{
			MethodName: "SendOfficerHint",
			Handler:    _Gateway_SendOfficerHint_Handler,
		},
		{
			MethodName: "ListCorpora",
			Handler:    _Gateway_ListCorpora_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "JoinAsViewer",
			Handler:       _Gateway_JoinAsViewer_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "aegis/v1/aegis.proto",
}

const (
	Engine_StreamTranscribe_FullMethodName = "/aegis.v1.Engine/StreamTranscribe"
	Engine_Health_FullMethodName           = "/aegis.v1.Engine/Health"
	Engine_ListCorpora_FullMethodName      = "/aegis.v1.Engine/ListCorpora"
)

// EngineClient is the client API for Engine service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type EngineClient interface {
	// Bidirectional stream for audio ingest and transcript egress.
	//
	// Lifecycle (per ADR-0005 R1 and ADR-0010 threading model):
	//  1. Gateway opens the stream.
	//  2. Gateway sends exactly one `SessionStart` as the first
	//     IngestMessage. The engine calls
	//     `ResourceBudget::Reserve(estimated_bytes)`. On failure, the
	//     engine returns RESOURCE_EXHAUSTED and closes the stream.
	//  3. Gateway sends an arbitrary sequence of `PcmChunk` and
	//     `ControlEvent` messages. The engine emits
	//     `TranscriptSegment`, `PrompterHint`, and `EngineStatus`
	//     messages as they become available.
	//  4. Gateway sends `ControlEvent{END_STREAM}` or closes the stream.
	//     The engine flushes any pending transcript and releases the
	//     ResourceBudget reservation. All per-session RAM is freed
	//     before the handler returns.
	//
	// Transient host disconnect (ADR-0006 WebRTC state transitions):
	//   - Gateway sends `ControlEvent{PAUSE}` on host
	//     `Disconnected`. The engine retains buffers, does NOT flush
	//     final segments, and stops emitting until `RESUME`.
	//   - Gateway sends `ControlEvent{RESUME}` when host returns to
	//     `Connected` within the 30-second grace window.
	//   - Gateway sends `ControlEvent{END_STREAM}` if the grace window
	//     expires (host `Failed`).
	//
	// Errors (wire form: grpc::Status):
	//
	//	INVALID_ARGUMENT      — malformed SessionStart or PcmChunk
	//	RESOURCE_EXHAUSTED    — ResourceBudget rejected the reservation
	//	INTERNAL              — whisper.cpp or diarization failure
	StreamTranscribe(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[IngestMessage, EgressMessage], error)
	// Health / readiness probe. Called by the Gateway for routing
	// decisions and by Kubernetes liveness / readiness probes.
	Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error)
	// Lists RAG collections visible to `tenant_id`. Backs the gateway's
	// public `ListCorpora` RPC. The engine filters Qdrant's
	// `CollectionsService.List()` output by prefix `aegis_<tenant_id>_`
	// (collection-level tenant isolation per ADR-0022 §Decision).
	//
	// Payload count / any other per-collection metadata is deliberately
	// NOT returned: Qdrant's list endpoint is O(1) name-only, but a
	// per-collection count would force N calls to `GetCollectionInfo`.
	// The viewer UI does not surface counts today — if that changes,
	// add a narrow `include_stats` request flag and pay the N+1 only
	// when asked.
	//
	// Errors:
	//
	//	INVALID_ARGUMENT      — empty tenant_id
	//	UNAVAILABLE           — Qdrant unreachable
	ListCorpora(ctx context.Context, in *EngineListCorporaRequest, opts ...grpc.CallOption) (*EngineListCorporaResponse, error)
}

type engineClient struct {
	cc grpc.ClientConnInterface
}

func NewEngineClient(cc grpc.ClientConnInterface) EngineClient {
	return &engineClient{cc}
}

func (c *engineClient) StreamTranscribe(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[IngestMessage, EgressMessage], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &Engine_ServiceDesc.Streams[0], Engine_StreamTranscribe_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[IngestMessage, EgressMessage]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Engine_StreamTranscribeClient = grpc.BidiStreamingClient[IngestMessage, EgressMessage]

func (c *engineClient) Health(ctx context.Context, in *HealthRequest, opts ...grpc.CallOption) (*HealthResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HealthResponse)
	err := c.cc.Invoke(ctx, Engine_Health_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *engineClient) ListCorpora(ctx context.Context, in *EngineListCorporaRequest, opts ...grpc.CallOption) (*EngineListCorporaResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EngineListCorporaResponse)
	err := c.cc.Invoke(ctx, Engine_ListCorpora_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EngineServer is the server API for Engine service.
// All implementations should embed UnimplementedEngineServer
// for forward compatibility.
type EngineServer interface {
	// Bidirectional stream for audio ingest and transcript egress.
	//
	// Lifecycle (per ADR-0005 R1 and ADR-0010 threading model):
	//  1. Gateway opens the stream.
	//  2. Gateway sends exactly one `SessionStart` as the first
	//     IngestMessage. The engine calls
	//     `ResourceBudget::Reserve(estimated_bytes)`. On failure, the
	//     engine returns RESOURCE_EXHAUSTED and closes the stream.
	//  3. Gateway sends an arbitrary sequence of `PcmChunk` and
	//     `ControlEvent` messages. The engine emits
	//     `TranscriptSegment`, `PrompterHint`, and `EngineStatus`
	//     messages as they become available.
	//  4. Gateway sends `ControlEvent{END_STREAM}` or closes the stream.
	//     The engine flushes any pending transcript and releases the
	//     ResourceBudget reservation. All per-session RAM is freed
	//     before the handler returns.
	//
	// Transient host disconnect (ADR-0006 WebRTC state transitions):
	//   - Gateway sends `ControlEvent{PAUSE}` on host
	//     `Disconnected`. The engine retains buffers, does NOT flush
	//     final segments, and stops emitting until `RESUME`.
	//   - Gateway sends `ControlEvent{RESUME}` when host returns to
	//     `Connected` within the 30-second grace window.
	//   - Gateway sends `ControlEvent{END_STREAM}` if the grace window
	//     expires (host `Failed`).
	//
	// Errors (wire form: grpc::Status):
	//
	//	INVALID_ARGUMENT      — malformed SessionStart or PcmChunk
	//	RESOURCE_EXHAUSTED    — ResourceBudget rejected the reservation
	//	INTERNAL              — whisper.cpp or diarization failure
	StreamTranscribe(grpc.BidiStreamingServer[IngestMessage, EgressMessage]) error
	// Health / readiness probe. Called by the Gateway for routing
	// decisions and by Kubernetes liveness / readiness probes.
	Health(context.Context, *HealthRequest) (*HealthResponse, error)
	// Lists RAG collections visible to `tenant_id`. Backs the gateway's
	// public `ListCorpora` RPC. The engine filters Qdrant's
	// `CollectionsService.List()` output by prefix `aegis_<tenant_id>_`
	// (collection-level tenant isolation per ADR-0022 §Decision).
	//
	// Payload count / any other per-collection metadata is deliberately
	// NOT returned: Qdrant's list endpoint is O(1) name-only, but a
	// per-collection count would force N calls to `GetCollectionInfo`.
	// The viewer UI does not surface counts today — if that changes,
	// add a narrow `include_stats` request flag and pay the N+1 only
	// when asked.
	//
	// Errors:
	//
	//	INVALID_ARGUMENT      — empty tenant_id
	//	UNAVAILABLE           — Qdrant unreachable
	ListCorpora(context.Context, *EngineListCorporaRequest) (*EngineListCorporaResponse, error)
}

// UnimplementedEngineServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEngineServer struct{}

func (UnimplementedEngineServer) StreamTranscribe(grpc.BidiStreamingServer[IngestMessage, EgressMessage]) error {
	return status.Errorf(codes.Unimplemented, "method StreamTranscribe not implemented")
}
func (UnimplementedEngineServer) Health(context.Context, *HealthRequest) (*HealthResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Health not implemented")
}
func (UnimplementedEngineServer) ListCorpora(context.Context, *EngineListCorporaRequest) (*EngineListCorporaResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method ListCorpora not implemented")
}
func (UnimplementedEngineServer) testEmbeddedByValue() {}

// UnsafeEngineServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EngineServer will
// result in compilation errors.
type UnsafeEngineServer interface {
	mustEmbedUnimplementedEngineServer()
}

func RegisterEngineServer(s grpc.ServiceRegistrar, srv EngineServer) {
	// If the following call pancis, it indicates UnimplementedEngineServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Engine_ServiceDesc, srv)
}

func _Engine_StreamTranscribe_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(EngineServer).StreamTranscribe(&grpc.GenericServerStream[IngestMessage, EgressMessage]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type Engine_StreamTranscribeServer = grpc.BidiStreamingServer[IngestMessage, EgressMessage]

func _Engine_Health_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HealthRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EngineServer).Health(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Engine_Health_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EngineServer).Health(ctx, req.(*HealthRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _Engine_ListCorpora_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EngineListCorporaRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EngineServer).ListCorpora(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Engine_ListCorpora_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EngineServer).ListCorpora(ctx, req.(*EngineListCorporaRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Engine_ServiceDesc is the grpc.ServiceDesc for Engine service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Engine_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "aegis.v1.Engine",
	HandlerType: (*EngineServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Health",
			Handler:    _Engine_Health_Handler,
		},
		{
			MethodName: "ListCorpora",
			Handler:    _Engine_ListCorpora_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "StreamTranscribe",
			Handler:       _Engine_StreamTranscribe_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "aegis/v1/aegis.proto",
}
