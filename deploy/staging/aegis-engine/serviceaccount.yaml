apiVersion: v1
kind: ServiceAccount
metadata:
  name: aegis-engine
  namespace: aegis
  labels:
    app.kubernetes.io/name: aegis-engine
    app.kubernetes.io/part-of: aegis-core
  # Engine has no AWS API surface today. Placeholder SA — future
  # IRSA annotation lands if/when the engine ever touches S3
  # (e.g., streaming model file download on cold start, per
  # ADR-0026 discussions). Not in C-1 scope.
