# Model binary files — downloaded via tools/scripts/download_models.sh,
# NOT checked into git. Integrity verified against manifest.json SHA-256.
*.gguf
*.bin
*.onnx
*.safetensors
*.pt
*.pth
*.ckpt

# Download staging area
cache/
downloads/
tmp/

# CAS model dirs — local dev symlinks onto external storage (T7 items dir),
# not tracked; content lives at AEGIS_MODEL_PATH regardless of link target.
/bge-m3-q4km
/whisper-tiny-en

# PGP signature (Phase 4+ — tracked separately when signing is enabled)
# manifest.json.sig
