// frontend_web/src/pages/Host/HostPage.tsx
//
// Phase 1 C1 placeholder. Phase 1 C4 flesh-out wires the
// AudioCaptureProvider + TranscriptStreamProvider into the real UI.

export function HostPage(): JSX.Element {
  return (
    <main>
      <h2>Host</h2>
      <p style={{ color: "#888" }}>
        Phase 1 C4 will wire up the <code>AudioCaptureProvider</code> and
        start/stop controls here.
      </p>
    </main>
  );
}
